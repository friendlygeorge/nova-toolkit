# contracts/

Sample contracts used for testing the toolkit — intentionally vulnerable where useful.

- `vulnerable/`     — known-bad contracts for detector validation
- `examples/`       — minimal reference implementations
