# tools/

Standalone CLI analysis utilities.

Each tool is self-contained:

```
tools/<tool-name>/
├── README.md
├── package.json   # or foundry.toml / pyproject.toml
├── src/           # or contracts/, python/
└── test/
```
